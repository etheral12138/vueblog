---
title: 蓝桥杯
date: 2023-3-5
icon: 
category:
- 前端技术
tag:
- Web
- 蓝桥杯
---



## 模拟赛第三期

### 第一题 网页PPT

考点：jquery,模板字符串

怎么会有模板字符串这么长的题啊(doge)

写着感觉像二叉树的中序遍历（orz）

```javascript
function switchPage() {
  // TODO: 请补充该函数，实现根据activeIndex切换页面的功能，并且在到达最后一页或第一页时给相应的按钮添加disable类
  activeIndex==0?$(".left").addClass("disable"):$(".left").removeClass("disable")
  activeIndex==4?$(".right").addClass("disable"):$(".right").removeClass("disable")
  $(".page").html(`${activeIndex+1}/5`)
  $(`section:lt(${activeIndex})`).css("display","none")
  $(`section:eq(${activeIndex})`).css("display","block")
  $(`section:gt(${activeIndex})`).css("display","none")
}
```

### 第二题 西游记之西天取经

考点：CSS中的animation

```css
.actor:first-child {
    width: 200px;
    height: 180px;
    background: url(../images/west_01.png) no-repeat;
    /* TODO 填空 */
    animation: a1 0.8s steps(8);
    animation-iteration-count: infinite;/*记住一定要写在animation后面*/
}
.actor:nth-child(2) {
    width: 200px;
    height: 180px;
    background: url(../images/west_02.png) no-repeat;
    /* TODO 填空 */
    animation: a2 0.8s steps(8);
    animation-iteration-count: infinite;
}
.actor:nth-child(3) {
    width: 170px;
    height: 240px;
    background: url(../images/west_03.png) no-repeat;
    /* TODO 填空 */
    animation: a3 0.8s steps(8); 
    animation-iteration-count: infinite;
}
.actor:last-child {
    width: 210px;
    height: 200px;
    background: url(../images/west_04.png) no-repeat;
    /* TODO 填空 */
    animation: a4 0.8s steps(8);
    animation-iteration-count: infinite;
}
```

### 第三题 商品销量和销售额实时展示看板



### 第四题 蓝桥校园一卡通

考点：JS中的DOM操作，正则表达式

```javascript
			let f=document.querySelectorAll(".form-group")
			let reg1 = /^[\u4e00-\u9fa5]{2,4}$/;
			let vailname=document.getElementById("vail_name")
			if(reg1.test(studentName.value.trim())==false){
				f[0].classList.add("has-error")
				vailname.style.display="inline"
			}
			else{
				f[0].classList.remove("has-error")
				vailname.style.display="none"
			}
            let reg2=/^\d{1,12}$/
			let vailid=document.getElementById("vail_studentId")
		    if(reg2.test(studentId.value.trim())==false&&reg1.test(studentName.value.trim())==true){
				f[1].classList.add("has-error")
				vailid.style.display="inline"
			}
			else{
				f[1].classList.remove("has-error")
				vailid.style.display="none"
			}
			if(reg2.test(studentId.value.trim())==true
             &&reg1.test(studentName.value.trim())==true)
            {
			item[0].innerHTML=`姓名:${studentName.value}`
			item[1].innerHTML=`学号:${studentId.value}`
            item[2].innerHTML=`学院:${college.value}`
			// 添加 showCard 类显示放大一卡通的动画，请勿删除
			cardStyle.classList.add('showCard') 
			}
```

### 第五题 会员权益领取中心



### 第六题 心愿便利贴

```javascript
<div class="container">
				<!-- TODO 待修改的代码 -->
				<div 
                 class="card" 
                :class="item.css" 
                 v-for="(item,index) in [](中括号改为wishList)" 
                :key="index">		
rules: {
					// TODO 待补充验证的代码
		  name: [
            { required: true, message: '请输入姓名', trigger: 'blur' },
            { min: 2, max: 4, message: '长度在 2 到 4 个字符', trigger: 'blur' }
          ],
		  content: [
            { required: true, message: '请输入内容', trigger: 'blur' },
            { min: 1, max: 30, message: '长度在 1 到 30 个字符', trigger: 'blur' }
          ]
	   },
```

### 第七题  消失的Token

考点：Vuex模块化

```javascript
        var app = new Vue({
            el: '#app',
            data() { },
            computed: {
                welcome() {
                    return store.getters.welcome
                },
                username() {
                    return store.getters["user/username"]
                },
                token() {
                    return store.getters["user/token"]
                }
            },
            methods: {
                // 回车/点击确认的回调事件
                login(username) {
                    username && store.commit('user/login', 
                    { username,token:'sxgWKnLADfS8hUxbiMWyb'}),
                    username && store.commit('say', '登录成功，欢迎你回来！')
                }
            }
        })
```

### 第八题 封装Promisefy函数

考点:JS中的Promise

```javascript
const promisefy = (fn) => {
  // TODO 此处完成该函数的封装
  return function (...args) {
      console.log(...args,"...args");
      return new Promise(function (resolve, reject) {
          // 将callback放到参数末尾,并执行callback函数
          args.push(function (err, ...arg) {
              console.log(...args,"12"); 
              if (err) {
                  reject(err);
                  return;
              }
              resolve(...arg);
          });
          fn.apply(null, args);
      });
  }
}
```

### 第九题 趣购



### 第十页 分页组件

```javascript
async function ajax({ url, method = "get", data, query: { currentPage, pageSize } }) {
    // TODO:根据函数参数 `query` 对象  `currentPage, pageSize` 获得当前页的数据
    let result = {
        data:[],
        total:0
    }
    axios({
        method: "get",
        url: url,
        params: {
            query:{
                currentPage,
                pageSize
            }
        },
      }).then((res)=>{
        result.data=res.data.data;
        result.total=res.data.total
      });
    //   console.log(result)
    return result;
}       
initEvents() {
       this.root.querySelector("#btn-prev").addEventListener('click', () => {
            // TODO:"<" 按钮的点击事件， 点击时 this.currentPage - 1
            if(this.currentPage==1){
                return
            }
            this.currentPage--;
        })
        this.root.querySelector("#btn-next").addEventListener('click', () => {
            // TODO:">" 按钮的点击事件， 点击时 this.currentPage + 1
            if(this.currentPage==this.totalPages){
                return
            }
            this.currentPage++;
        })
const createPaginationIndexArr = (currentPage, totalPages, pagerCount) => {
    let indexArr = [];
    // TODO：根据传参生成分页数组 indexArr
    indexArr.push(currentPage)
    indexArr.length=totalPages;
    if(totalPages<=pagerCount){
        indexArr.push(currentPage)
    }
    if(totalPages>pagerCount){

      indexArr.push(currentPage)
      indexArr.push(totalPages)
    }
    return indexArr;
}
if(indexArr[])
        let template =`<li class="number active">1</li>
        <li class="number ">2</li>
        <li class="number ">3</li>
        <li class="number ">4</li>
        <li class="number more">...</li>
        <li class="number ">10</li>`;
 
```



### 总结

这次模拟赛让我明白了自己的不足，比如jquery的书写不熟练，Promise的理解不到位。继续努力吧！
